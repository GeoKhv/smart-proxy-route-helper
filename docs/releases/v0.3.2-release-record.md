# Smart Proxy Route Helper v0.3.2 Release Record

## Release identity

| Field | Verified value |
| --- | --- |
| Release version | `0.3.2` |
| Release commit | `079fd565ac85e34388f51f0dfe38d796b77bdcf1` |
| Annotated tag object | `a6a718cf2776a991bd052f4c19cd18be50c0504c` |
| Annotated tag peeled target | `079fd565ac85e34388f51f0dfe38d796b77bdcf1` |
| GitHub Release | Public `v0.3.2`: https://github.com/GeoKhv/smart-proxy-route-helper/releases/tag/v0.3.2 |
| Chrome Web Store | Production version `0.3.2` published |
| CI | GitHub Actions run `32182320069`; push workflow succeeded on the release commit |
| Release ZIP | `release/smart-proxy-route-helper-v0.3.2.zip` |
| Final ZIP size | `640892` bytes |
| SHA-256 | `b0708afedac252b42edc2f5f2c026cdfe40cc108351df597048866a9f20e52c3` |
| Packaged files | 15 |

This is the final production publication record for `0.3.2`. The annotated tag, public GitHub
Release, and Chrome Web Store production publication all refer to the verified `0.3.2` release.

## Automated checks

The following checks passed for the release tree:

| Check | Result |
| --- | --- |
| `npm test` | Pass; 355 tests passed |
| `npm run typecheck --if-present` | Pass |
| `npm run build` | Pass; built manifest reports `0.3.2` |
| `npm run package` | Pass; created `release/smart-proxy-route-helper-v0.3.2.zip` |
| `npm run check:release-surface` | Pass; 15 packaged files |
| EN/RU locale parity | Pass; 480 matching message keys |
| `npm audit --audit-level=high` | Pass; the known low-severity `esbuild` advisory remains |
| `git diff --check` | Pass |
| GitHub Actions CI | Pass; run `32182320069` succeeded for `079fd565ac85e34388f51f0dfe38d796b77bdcf1` |

The packaged permissions are exactly `proxy`, `storage`, `activeTab`, and `scripting`.
No `host_permissions` are present.

The archive contains only the expected compiled release surface: root manifest, background,
Popup, Options, shared runtime chunks, English and Russian locales, and icons. It does not
contain source files, tests, docs, `node_modules`, temporary files, or a nested `dist/` directory.

The published package is `640892` bytes with SHA-256
`b0708afedac252b42edc2f5f2c026cdfe40cc108351df597048866a9f20e52c3` and 15 files.

## Confirmed manual smoke

The release owner manually confirmed the following on the affected Chrome profile:

- Chrome Sync Add → Edit → Delete for routing rules;
- Pause → Resume routing;
- final visual smoke switching between Active and Paused.

## Publication status

- Annotated tag `v0.3.2` exists and peels to `079fd565ac85e34388f51f0dfe38d796b77bdcf1`.
- GitHub Release `v0.3.2` is public, not a draft, and not a prerelease.
- The GitHub Release contains exactly one asset: `smart-proxy-route-helper-v0.3.2.zip`.
- Chrome Web Store production version `0.3.2` is published.
- The Chrome Web Store package is the same verified release artifact represented by the size and SHA-256 above.
