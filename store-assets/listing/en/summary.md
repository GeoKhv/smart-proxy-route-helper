# English Store Summary

Extension title:

```text
Smart Proxy Route Helper
```

## Primary

```text
Routes selected sites through your local proxy while all other sites connect directly.
```

Character count: 86 Unicode characters, including spaces and punctuation.

## Short fallback

```text
Selected sites use your local proxy; all other sites connect directly.
```

Character count: 70 Unicode characters, including spaces and punctuation.

Both variants are within Chrome Web Store's 132-character summary limit.

The English runtime `extensionDescription` is the primary summary above. The Russian runtime value and Russian Store summary are both:

```text
Направляет выбранные сайты через локальный прокси, а остальные открывает напрямую.
```

This is a candidate listing package for v0.3.0; it does not change the manifest or package version.
