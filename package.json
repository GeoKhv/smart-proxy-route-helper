{
  "name": "smart-proxy-route-helper",
  "version": "0.0.0",
  "private": true,
  "description": "Initial Manifest V3 TypeScript scaffold for Smart Proxy Route Helper.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.321",
    "@types/node": "^24.0.3",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
