# Smart Proxy Route Helper v0.3.1 Development Record

## Release identity

| Field | Verified value |
| --- | --- |
| Current development version | `0.3.1` on `main` |
| Latest immutable GitHub Release | `v0.3.0` |
| Chrome Web Store package | `v0.3.0`; submitted for review and awaiting review |
| `v0.3.1` tag | None |
| `v0.3.1` GitHub Release | None |
| `v0.3.1` Chrome Web Store submission | None |

Version `0.3.1` is a development version on `main`, not a published release. The immutable
`v0.3.0` tag and GitHub Release remain the latest release artifacts. The Chrome Web Store
`v0.3.0` package remains submitted for review and awaiting review; this record does not claim
that it is published.

## Automated checks

The following checks passed for the `0.3.1` development tree during this documentation closeout:

| Check | Result |
| --- | --- |
| `git diff --check` | Pass |
| `npm test` | Pass |
| `npm run typecheck` | Pass |
| `npm run build` | Pass; built manifest reports `0.3.1` |
| `npm run check:release-surface` | Pass; 15 packaged files |
| `npm run package` | Pass; created `release/smart-proxy-route-helper-v0.3.1.zip` |

The manifest version remains `0.3.1`. Packaged permissions remain exactly `proxy`, `storage`,
`activeTab`, and `scripting`; no host permissions, `<all_urls>`, `webRequest`, or `webNavigation`
were added.

The development dependency tree retains the known low-severity, dev-only `esbuild` advisory
`GHSA-g7r4-m6w7-qqqr`. No dependency update is included in this record.

## Local package verification

| Field | Verified value |
| --- | --- |
| Asset | `release/smart-proxy-route-helper-v0.3.1.zip` |
| Size | `615861` bytes |
| SHA-256 | `f5102809e199a3853756bba729783c0635d55c8eeed8a38183e206cb821e35d4` |
| Packaged files | 15 |

The archive has `manifest.json` at its root and contains the compiled background, Popup, Options,
shared chunks, locales, and icons. It does not contain source files, tests, docs, `node_modules`,
temporary files, or a nested `dist/` directory.

## Confirmed manual smoke

On `0.3.1`, the release owner manually confirmed that all of the following scenarios completed
correctly, with no errors or regressions observed:

- exact-rule routing;
- include-subdomains routing;
- direct connection for sites without a rule;
- `Check via proxy`;
- changing routing state while diagnostics are running;
- restoring the latest applicable PAC after diagnostics;
- restarting diagnostics without hanging;
- exporting and importing settings;
- import messages in Russian and English;
- `Auto`, `English`, and `Русский` language selection;
- adding and removing a domain;
- upgrading `exact` to `include-subdomains`;
- duplicate prevention;
- mixed batch handling.

This manual confirmation covers the current `0.3.1` development build. It does not create or
imply a tag, GitHub Release, Chrome Web Store submission, or publication for `0.3.1`.

## Scope and publication boundary

This record documents the successful manual smoke only. It does not change runtime code, tests,
the version, dependencies, tags, GitHub Releases, release assets, or Chrome Web Store state.
